# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.

type: charm
platforms:
  ubuntu@24.04:amd64:
parts:
  charm:
    plugin: charm

name: test-charm-velero
description: |
  The application charm used in integration tests.
summary: |
  The charm is only meant to be used for testing of the velero_backup_config lib in this repository.

config:
  options:
    ttl:
      description: "Test TTL config"
      default: "24h5m5s"
      type: string
    schedule:
      description: "Cron schedule for backups (optional, empty means no schedule)"
      default: ""
      type: string
    paused:
      description: "Whether the schedule is paused"
      default: false
      type: boolean

provides:
  first-velero-backup-config:
    interface: velero_backup_config
  second-velero-backup-config:
    interface: velero_backup_config
